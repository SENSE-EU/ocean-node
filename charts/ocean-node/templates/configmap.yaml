apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ocean-node.fullname" . }}
  labels:
    {{- include "ocean-node.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
